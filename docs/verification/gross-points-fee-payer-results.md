# 粗点表示＋場代立替モデル＋設定メニュー 検証結果

実施日: 2026-06-12 / 対応プラン: docs/plans/gross-points-fee-payer-plan.md
検証環境: ローカル dev サーバー（実 Supabase 接続）、viewport 390×844（モバイル）

## 1. 静的検証

| # | 項目 | 結果 | 根拠 |
|---|------|------|------|
| 1 | `npm run test` | **PASS** | `Test Files 1 passed (1)` / `Tests 46 passed (46)`（旧30＋新16） |
| 2 | `npm run build` | **PASS** | `✓ built in 309ms`、型エラー0 |
| 3 | `npm run lint` | **PASS** | eslint 出力なし（エラー0） |

## 2. UI/E2E 検証（preview ツールによる実機観測）

| # | 項目 | 結果 | 観測根拠（一次情報） |
|---|------|------|---------------------|
| 1 | 局ごとの粗点表示（42300/28200/18000/11500、点5・ウマ10-20・オカあり） | **PASS** | 行収支 `+53 / +8 / △22 / △39`（accessibility snapshot で確認。手計算: 五捨六入後 42000/28000/18000/11000 → 52/8/−22/−39、和−1をトップ調整→53） |
| 2 | 累計収支の粗点表示 | **PASS** | `.cumulative-amount` = `["+53","+8","△22","△39"]` |
| 3 | スコア表示部に「円」が出ない | **PASS** | table 内の「円」は場代行の単位表示1箇所のみ（意図的に維持）。収支セル・累計はすべて粗点 |
| 4 | 粗点の正負色分け | **PASS** | computed style: `.pt-plus` color=rgb(29,107,78)（緑系）、`.pt-minus` color=rgb(179,64,47)（赤系） |
| 5 | ⚙設定ボタンの表示と遷移 | **PASS** | ヘッダーに `⚙ 設定`（aria-label='設定を変更'）、タップで設定画面へ遷移 |
| 6 | 設定編集モード | **PASS** | 見出し「ルール設定を変更」、プレイヤー名入力非表示、現在値復元（点5/10-20/あり/均等割り が選択状態）、ボタン「この設定で続ける」 |
| 7 | 設定変更の反映（点5→点10） | **PASS** | 保存後ゲーム画面に復帰。粗点はレート非依存で不変（正しい）。清算画面の円換算が点10で算出（+53pt→受取¥5,300） |
| 8 | 清算画面は円表示のみ | **PASS** | 送金 `P4→P1 3,900円 / P3→P1 1,400円 / P3→P2 800円`（=ポイント×100 と完全一致、ゼロサム） |
| 9 | 立替者選択UI（未選択時） | **PASS** | 「店への支払い」カード＋「場代を払う人」ボタン4つ＋注記「場代を払う人を選ぶと、立替分を含めた送金額になります（現在は麻雀収支のみ）」を確認 |
| 10 | 新規作成モードの回帰 | **PASS** | 本検証の卓作成自体が改修後コードの新規フロー（プレイヤー名表示・卓作成・合言葉発行 XRHFYD）で成功 |
| 11 | コンソールエラー | **PASS** | preview_console_logs (warn/error) = なし |
| 12 | 立替者保存失敗時のエラー表示（追加改修・2026-06-13） | **PASS** | 保存失敗時に `.fee-payer-error`（role=alert、color=rgb(179,64,47)）で「場代を払う人を保存できませんでした。サーバー側の更新（fee_payer_id 列の追加）が済んでいるか確認してください。」を表示し、選択はロールバック。実機観測済み |

## 3. 保留項目（ブロッカーあり）

| # | 項目 | 状態 | 理由 |
|---|------|------|------|
| P1 | 立替者選択の保存＋立替込み送金・「○○さんが店に払う」カード表示 | **解消 → PASS (2026-06-13)** | ユーザーが ALTER 実行後に E2E 実施。選択保持・カード表示・立替込み送金とも手計算一致を確認（詳細: docs/verification/personal-expense-items-results.md セクション3 E3-E5） |
| P2 | スクリーンショット | **取得不可** | preview_screenshot が30秒タイムアウト（Phase 2 と同一のツール障害、2回再現）。テキストベース観測（snapshot / computed style）で代替。ユーザーの目視確認を依頼 |

## 4. 特記事項

- 検証中、合成イベント（連続blur）により同一行が二重確定される事象があったが、これは検証操作の副作用（実機のタップ操作では発生しない）。行削除機能（×ボタン）で正常に削除できることも副次的に確認
- 検証用の卓（合言葉 XRHFYD）が本番DBに残存。実害なし（ルーム削除手段なしは既知の P3 候補）
